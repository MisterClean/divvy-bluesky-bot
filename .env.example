# Safety: publishing remains off unless this is explicitly true.
PUBLISH_ENABLED=false
POST_LIMIT=10

# Bluesky
BLUESKY_SERVICE_URL=https://bsky.social
BLUESKY_IDENTIFIER=divvystationbot.bsky.social
BLUESKY_APP_PASSWORD=

# Database
DB_PATH=data/divvy-bot.sqlite3

# Divvy source
SODA_URL=https://data.cityofchicago.org/resource/bbyy-e7gq.json
SODA_TIMEOUT_MS=30000
SODA_MAX_RETRIES=3
SODA_MIN_STATIONS=500

# Protomaps / static map rendering
PROTOMAPS_KEY=
# Optional override for testing or self-hosting:
# PROTOMAPS_STYLE_URL=https://example.com/style.json
MAP_WIDTH=1080
MAP_HEIGHT=1350
# Keep the prepared announcement-card output at exactly 1080x1350.
MAP_PIXEL_RATIO=1
MAP_ZOOM=17
MAP_NIGHTLINE_ZOOM=17.5
MAP_RENDER_TIMEOUT_MS=75000
MAP_RENDER_MAX_ATTEMPTS=2
MAP_RENDER_RETRY_DELAY_MS=2000
# Set this explicitly in containers that use a system Chromium package.
# AGENT_BROWSER_EXECUTABLE_PATH=/usr/bin/chromium-browser
OUTPUT_DIR=output

# Google Street View
STREETVIEW_ENABLED=false
GOOGLE_MAPS_API_KEY=
STREETVIEW_TIMEOUT_MS=10000
